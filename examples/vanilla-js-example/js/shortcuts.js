// Initialize NavigatorX
const nav = new window.NavigatorX();

// Make it globally available
window.navigatorInstance = nav;

// Register global shortcuts (will be set up by navigation.js)
console.log('✅ NavigatorX initialized');